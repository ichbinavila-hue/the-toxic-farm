name: The Toxic Farm - Web

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Export Godot Web
        uses: firebelley/godot-export@v8.0.0
        with:
          godot_executable_download_url: https://downloads.godotengine.org/?version=4.5.2&flavor=stable&slug=linux.x86_64.zip&platform=linux.x86_64
          godot_export_templates_download_url: https://downloads.godotengine.org/?version=4.5.2&flavor=stable&slug=export_templates.tpz&platform=templates
          relative_project_path: ./
          use_preset_export_path: true
          presets_to_export: Web
          cache: true

      - name: Verify and normalize Web export
        run: |
          echo "=== Web export files ==="
          find web -maxdepth 4 -type f | sort | head -200
          if [ ! -f web/index.html ] && [ -f web/Web/index.html ]; then
            echo "Found nested export at web/Web; moving it to web root."
            cp -a web/Web/. web/
          fi
          test -f web/index.html
          test -s web/index.html
          touch web/.nojekyll
          echo "Web export verified: web/index.html"

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: web

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
